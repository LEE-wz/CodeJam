# Recording — Phase 15 adaptive-auction fallback

This is the portable, content-free evidence report for the completed real-Agent
Session `40f52425-ea3d-4a9c-917e-b05e08c27128`, displayed on the prepared
release host as **[RECORDING] Phase 15 adaptive-auction fallback**. The source
checkpoint was `a383389`; Phase 15 changes only the scale/release surface and
does not alter this archived run.

It is a recording, not a simulation and not a promise that a fresh model run
will choose the same valid plan. Runtime data is intentionally not committed.

## Recorded rounds

| Round | Real elapsed time | Result |
|---|---:|---|
| Auto Direct | 4.3 s | one scored candidate published directly |
| Explicit Auction | 35.3 s | ten valid bids settled and one immutable award was committed |
| Auto escalation | 145.1 s | the primary candidate expanded into a bounded auction |
| Awarded sequential | 325.2 s | three execution messages committed in plan position order |
| Awarded parallel | 291.3 s | three execution turns ran as a bounded fan-out |
| Partial bidder contention | 42.7 s | the deliberately occupied late bidder settled without leaked work |
| Stop | 1.5 s | active work was fenced and the Session returned to `awaiting_input` |
| Resume | 3.4 s | the same durable Session accepted a later message |
| Post-bid/pre-award restart | 39.6 s | ten settled bids survived and produced exactly one award |

## Durable evidence summary

- 82 turns, 94 attempts, 84 artifacts, and 306 gapless events.
- 94 real provider calls: 4,714,116 input tokens, 4,449,536 cached input tokens,
  and 233,747 output tokens. These are usage counts, not cost claims.
- 22 genuine `invalid_output` attempts occurred on `session_bid` turns.
  Multiple bidders subsequently committed on a same-turn retry; invalid output
  never entered the public transcript.
- The restart boundary retained all ten settled bids, committed exactly one
  award naming a settled bid, executed it once, and left zero attempts running.
- The multi-prompt transcript survived Stop/resume and process restart.

## Security scope

This report deliberately contains no user prompt content, raw model output,
credentials, authorization data, provider thread identifiers, or lease tokens.
The release inspection separately verified that coordination events use bounded
metadata only and that user-authored content appears only in user-message
artifacts and the transcript.

## Reproduction

After completing the root README Docker Compose setup and configuring genuine
Agents, run the full provider-backed rehearsal:

```bash
node scripts/pa14-27-rehearsal.mjs run
```

It creates its own ten-Agent roster, exercises contention, Stop/resume, and the
post-bid/pre-award restart boundary, and may take well over ten minutes. Fresh
model output is nondeterministic; invalid-output recovery is therefore observed
evidence, not a forced response.

If this recorded run is retained in local runtime state, regenerate the safe
API-backed report with:

```bash
node scripts/pa14-27-rehearsal.mjs report \
  40f52425-ea3d-4a9c-917e-b05e08c27128
```
